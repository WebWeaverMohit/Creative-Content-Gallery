<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Creative Content Gallery</title>

  <!-- Remix Icons -->
  <link href="https://cdn.jsdelivr.net/npm/remixicon@4.0.0/fonts/remixicon.css" rel="stylesheet" />
  
  <!-- Google Font: Outfit -->
  <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@400;600;700&display=swap" rel="stylesheet" />
  
  <!-- Tailwind CSS -->
  <script src="https://cdn.tailwindcss.com"></script>
  <script>
    tailwind.config = {
      theme: {
        extend: {
          fontFamily: {
            outfit: ['Outfit', 'sans-serif'],
          },
          colors: {
            primary: '#3C486B',
            secondary: '#F45050',
            cream: '#FAF4EF',
            mustard: '#F9D949',
          },
        },
      },
    };
  </script>

  <!-- Your Custom CSS -->
  <link rel="stylesheet" href="/stylesheets/style.css" />
</head>

<body class="bg-cream font-outfit text-primary">
  <% if(nav === true){ %>
  <div class="w-full px-10 py-3 flex justify-between items-center bg-white/70 backdrop-blur-md shadow-md sticky top-0 z-50 rounded-b-xl">
    <h2 class="text-xl font-semibold">Creative Content Gallery</h2>
    <div class="flex gap-6 items-center text-[1rem] font-medium">
      <a href="/profile" class="hover:text-secondary transition-colors">Profile</a>
      <a href="/feed" class="hover:text-secondary transition-colors">Feed</a>
      <a href="/logout" class="px-4 py-2 bg-secondary text-white rounded-xl shadow-sm hover:scale-105 transition-transform">Logout</a>
    </div>
  </div>
  <% } %>
